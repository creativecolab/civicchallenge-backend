language: php

services:
  - mysql

php:
  - 5.6
  - 7.0
  - 7.1

before_install:
  - composer self-update

install:
  - composer install --no-interaction

before_script:
  - cp .env.travis .env
  - php artisan migrate
  - mysql -e 'CREATE DATABASE test;'

script:
  - vendor/bin/phpunit